---
name: 17-scys-manual-collector
description: 收集或增量更新用户本人已购买或以其他合法方式取得访问权限的生财有术航海手册 Plain Text 区块，并整理成带承上启下说明的 Obsidian 或普通 Markdown 手册。用户要求整理、收集、同步或更新本人有权访问的生财航海手册时使用；优先调用可用的 scys-mcp 手册目录与正文工具，没有连接器时回退到已登录网页的 DOM、复制或下载能力。不用于全文搬运、公开传播课程内容、帖子搜索或普通网页摘录。
---

# 生财航海手册整理 Skill（非官方）

## 使用资格与非官方声明

这是一个独立开发的非官方本地整理工具，与生财有术及其关联主体不存在隶属、合作、授权或背书关系。

只处理用户本人已经购买，或以其他合法方式取得访问权限的手册页面。当前上下文没有明确权限信息时，先让用户确认；用户没有访问权限、要求绕过权限，或要求公开传播课程整理结果时停止。

Skill 源码和分享包不包含、托管或分发任何课程正文、图片、音视频、账号凭据或访问令牌，也不能替代购买原课程。

## 交付结果

把一份生财航海手册整理成可维护的 Markdown 文件夹：

- 一本手册一个文件夹；
- 一个精简的 `00-手册总览与SOP.md`；
- 一个二级“关卡 / 章节”对应一份笔记；
- 只收集页面明确标记为 `Plain Text` 的区块；
- 周围标题与正文只用于判断顺序、分支和编写承上启下说明；
- Plain Text 原文逐字保留，说明文字必须由网页内容直接支持。

不要把手册全文、图片、普通代码块、签名链接或用户聊天写进结果。

## 开始前必须确认

先确认以下信息；缺失且无法从当前上下文安全解析时，一次只问一个问题：

1. 用户已经购买或以其他合法方式取得目标手册的访问权限；
2. 目标手册链接、航海名称或可验证的活动标识；
3. 输出到 Obsidian Vault 还是普通 Markdown 文件夹；
4. 真实输出目录；
5. 是首次收集还是更新现有手册。

读取目标目录适用的 `AGENTS.md` 或同类本地规则。Skill 自身绝不生成 `## 原话记录`；如果目标规则强制要求该章节，停止在预览阶段，报告冲突并让用户决定，不要静默添加。

## 选择取数通道

每次执行都先检查当前可调用工具，不凭插件名称或历史会话假设能力存在。

### A. scys-mcp 模式

当活动搜索或列表、手册目录和手册正文能力均可调用时，优先使用此模式。读取 [references/scys-mcp-mode.md](references/scys-mcp-mode.md)，按目录完整扫描，不使用站内搜索框。

### B. 已登录网页回退模式

缺少上述连接器能力时，读取 [references/browser-fallback.md](references/browser-fallback.md)。优先读取页面底层结构和 DOM；只有 DOM 无法取得原文时，才点击 Plain Text 区块的复制或下载按钮。

如果网站要求登录，停下来请用户在选定浏览器中完成登录。不要读取或导出 Cookie、Token、localStorage、密码或浏览器配置。

## 组织内容

读取 [references/output-contract.md](references/output-contract.md)，据此建立文件计划。

关键规则：

- 一级“篇”只作为总览分组；
- 二级“关卡 / 章节”各生成一份笔记；
- 三级小节合并到所属关卡笔记；
- 平铺文章列表则每篇一份笔记；
- 每条 Plain Text 前写简短过渡，说明上一状态、当前原因和下一结果；
- 主流程按顺序写，互斥分支明确“选择其一”，示例明确“参考”，异常路径明确触发条件；
- 每段一至两句并用空行分隔；
- 不使用“用途 / 何时使用 / 来源 / blockId / 前置条件 / 人工关口”字段。

不得根据常识补课程没有说过的动作。资料不足时缩短说明或标记待确认，不编造承接关系。

## 写入前预览

先在临时位置生成候选文件，不直接覆盖目标目录。比较本地现状后向用户展示：

- `新增`：本地不存在；
- `需要更新`：同名文件的候选内容不同；
- `保持不动`：候选内容一致；
- `本地保留`：本地存在但网页计划中没有，明确不会自动删除。

同时展示手册文件夹、总览、关卡笔记的拟创建清单，以及收集到的 Plain Text 总数。只有用户明确确认后，才写入用户选中的“新增 / 需要更新”文件。

## 写入与验证

写入时只处理用户确认的精确文件。不要自动删除、移动、重命名或清理旧笔记；更新前保留可恢复的临时备份，验证通过后再清理临时产物。

写入后运行：

```bash
python3 <skill-folder>/scripts/validate_output.py <手册文件夹> --mode <obsidian|markdown> --expected-plain-text <数量>
```

还要人工核对：

- 目录结构和关卡顺序；
- 每个 Plain Text 与抓取原文逐字一致，包括末尾换行；
- 所有内部链接可解析；
- 没有 `## 原话记录`、个人路径、认证信息或签名媒体 URL；
- 只有用户确认的文件发生变化。

最后报告新增、更新、保持不动、本地保留、Plain Text 数量，以及无法验证的部分。

## 安全边界

- 不使用生财站内搜索框寻找 Plain Text；
- 不绕过登录、权限、付费墙、安全提示或验证码；
- 不为没有访问权限的用户获取内容，也不协助公开传播、出售或共享生成的课程笔记；
- 不点击“完成章节”、评论、点赞、收藏、发布等写操作；
- 不把浏览器凭据带到命令、日志、笔记或分享包；
- 检测到疑似账号、密钥、个人绝对路径或客户敏感信息时，写入前停止并让用户选择，不静默改写原文；
- 分享 Skill 源码、README、示例、测试、Issue 模板和发布材料中不得包含任何真实课程正文、个人 Vault 路径、聊天原话或运行日志。
